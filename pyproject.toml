[project]
name = "abn-invoice"
version = "0.1.0"
description = "Local invoice and reimbursement tool for Australian sole traders"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "jinja2>=3.1",
    "keyring>=25.0",
]
